<%= simple_form_for @screen do |f| %>
  <%= f.input :name, label: 'Nome' %>
  <%= f.input :address, label: 'Endereço', placeholder: @screen.user.address %>
  <%= f.input :capacity, label: 'Capacidade' %>
  <%= f.submit %>
<% end %>
