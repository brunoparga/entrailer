<div class="cards">
  <% @screenings.each do |screening| %>
    <%= render 'shared/cards',
                 screening: screening,
                 film: screening.detailed_film.film_title,
                 display_title: true %>
  <% end %>
</div>
